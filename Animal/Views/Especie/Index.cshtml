@model IEnumerable<Animal.Models.Especie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nomeVulgar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nomeCientifico)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nomeVulgar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nomeCientifico)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idEspecie }) |
            @Html.ActionLink("Details", "Details", new { id=item.idEspecie }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idEspecie })
        </td>
    </tr>
}

</table>
