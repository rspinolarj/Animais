@model IEnumerable<Animal.Models.IdentificadorAnimal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.codigoIdentificador)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Animal.idAnimal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoIdentificador.descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.codigoIdentificador)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Animal.idAnimal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoIdentificador.descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idIdentificadorAnimal }) |
            @Html.ActionLink("Details", "Details", new { id=item.idIdentificadorAnimal }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idIdentificadorAnimal })
        </td>
    </tr>
}

</table>
